# A题：水泥烧成系统电除尘器的协同优化控制 — 解题经过

## 一、题目分析

### 1.1 题目背景

某水泥企业为一条 $$5000$$ 吨/日新型干法生产线窑尾配套电除尘器，该除尘器共有 $$4$$ 个电场，每个电场配有独立的整流变压器，可调节二次电压设定值 $$U_i$$（$$\text{kV}$$）和振打周期 $$T_i$$（$$\text{s}$$）。附件中给出该除尘器连续 $$7$$ 天的运行数据，要求建立数学模型解决四个问题。

### 1.2 数据特征

数据文件 `Cement_ESP_Data.csv` 包含 $$10080$$ 行（分钟级，$$7$$ 天）、$$14$$ 列：

| 字段 | 含义 | 单位 |
|------|------|------|
| $$\text{timestamp}$$ | 时间戳 | 分钟级 |
| $$T_{in}$$ | 烟气入口温度 | $$\text{℃}$$ |
| $$C_{in}$$ | 入口粉尘浓度 | $$\text{g/Nm}^3$$ |
| $$Q$$ | 烟气流量 | $$\text{Nm}^3\text{/h}$$ |
| $$U_1 \sim U_4$$ | 各电场二次电压 | $$\text{kV}$$ |
| $$T_1 \sim T_4$$ | 各电场振打周期 | $$\text{s}$$ |
| $$C_{out}$$ | 出口粉尘浓度 | $$\text{mg/Nm}^3$$ |
| $$P_{total}$$ | 总除尘电耗 | $$\text{kW}$$ |

**关键数据特征**：$$C_{out}$$ 有 $$50$$ 个缺失值，历史标准差仅 $$0.17$$，集中在 $$50$$ $$\text{mg/Nm}^3$$ 附近，疑似传感器限幅。这意味着纯数据驱动回归外推不可靠，需以机理模型为主。

### 1.3 四个问题概述

- **问题1**：分析入口条件与操作参数对出口浓度的关系，研究振打周期对瞬时排放峰值的影响
- **问题2**：工况划分 + 约束优化（$$C_{out} \leq 10$$ $$\text{mg/Nm}^3$$ 下最小化电耗）
- **问题3**：选取2个差异工况对比，灵敏度分析优先级
- **问题4**：排放收紧至 $$5$$ $$\text{mg/Nm}^3$$，定量分析电耗增幅

---

## 二、数学建模框架

### 2.1 变量与常量定义

#### 决策变量（可控）

| 符号 | 含义 | 单位 | 范围 |
|------|------|------|------|
| $$U_i$$ | 第 $$i$$ 电场二次电压 | $$\text{kV}$$ | $$i=1,2,3,4$$ |
| $$T_i$$ | 第 $$i$$ 电场振打周期 | $$\text{s}$$ | $$i=1,2,3,4$$ |

#### 工况变量（不可控输入）

| 符号 | 含义 | 单位 |
|------|------|------|
| $$T_{in}$$ | 烟气入口温度 | $$\text{℃}$$ |
| $$C_{in}$$ | 入口粉尘浓度 | $$\text{g/Nm}^3$$ |
| $$Q$$ | 烟气流量 | $$\text{Nm}^3\text{/h}$$ |

#### 输出变量

| 符号 | 含义 | 单位 |
|------|------|------|
| $$C_{out}$$ | 出口粉尘浓度 | $$\text{mg/Nm}^3$$ |
| $$P_{total}$$ | 总除尘电耗 | $$\text{kW}$$ |

#### 中间变量

| 符号 | 含义 |
|------|------|
| $$\eta_i$$ | 第 $$i$$ 电场单级除尘效率 |
| $$C_i$$ | 第 $$i$$ 电场出口浓度（$$C_0 = C_{in}$$） |
| $$P_i$$ | 第 $$i$$ 电场电耗 |
| $$C_{peak}$$ | 振打瞬时排放峰值 |

#### 常量

| 符号 | 值 | 含义 |
|------|----|------|
| $$C_{limit}$$ | $$10$$ | 超低排放标准 ($$\text{mg/Nm}^3$$) |
| $$C_{limit}'$$ | $$5$$ | 收紧后标准 ($$\text{mg/Nm}^3$$) |
| $$n$$ | $$4$$ | 电场数量 |
| $$U_{min,i}, U_{max,i}$$ | 由数据定 | 电压上下限 |
| $$T_{min,i}, T_{max,i}$$ | 由数据定 | 振打周期上下限 |
| $$T_{crit,i}$$ | 历史95分位 | 振打安全上限 |
| $$k_i$$ | 待拟合 | 第 $$i$$ 电场电耗系数 |

### 2.2 等式与不等式

#### 问题1：关系建模

**多级串联等式**：

$$C_i = C_{i-1} \cdot (1 - \eta_i), \quad i=1,2,3,4 \tag{2}$$

$$C_{out} = C_4 \cdot 1000 \quad (\text{g} \to \text{mg转换}) \tag{3}$$

**Deutsch方程（单级效率）**：

$$\eta_i = \left(1 - \exp\left(-\frac{k_i A_i \cdot U_i^2}{Q}\right)\right) \cdot \exp\left(-\alpha_i \cdot d_i\right) \tag{4}$$

其中双向偏离惩罚：

$$d_i = \begin{cases} T_i - T_{ref,i}, & T_i \geq T_{ref,i} \\ r \cdot (T_{ref,i} - T_i), & T_i < T_{ref,i} \end{cases}, \quad r = 0.5$$

其中：
- $$k_i A_i$$：合并系数（待拟合，共享 $$kA_0$$，$$kA_i = kA_0 \cdot g_i$$，$$g=[1,1,0.9,0.9]$$）
- $$\alpha_i$$：振打衰减系数（下界 $$0.001$$）
- $$T_{ref,i}$$：参考振打周期（取历史中位数）
- $$r$$：振打过频副作用占比，固定 $$0.5$$（$$C_{out}$$ 限幅致不可辨识）

**电耗等式**：

$$P_{total} = \sum_{i=1}^{4} P_i = \sum_{i=1}^{4} k_i \cdot U_i^2 \tag{6}$$

当 $$R^2 < 0.9$$ 时扩展为（约束 $$k_i \geq 0,\, \beta_i \geq 0$$，用 `lsq_linear` TRF）：

$$P_{total} = \sum_{i=1}^{4} k_i U_i^2 + \sum_{i=1}^{4} \frac{\beta_i}{T_i} + c \tag{6'}$$

其中 $$\beta_i/T_i$$ 为振打电机功耗（每次振打能量 $$\times$$ 频率 $$1/T_i$$）。

**振打峰值等式**（双向偏离）：

$$C_{peak} = \sum_{i=1}^{4} \alpha_i \cdot d_i \cdot C_{in} \cdot 1000 \cdot 0.1 \tag{5}$$

#### 问题2：工况划分 + 约束优化

**工况划分**：将 $$(C_{in}, T_{in}, Q)$$ 空间划分为 $$K$$ 个典型工况 $$\Omega_k$$：

$$\bigcup_{k=1}^{K} \Omega_k = \Omega_{all}, \quad \Omega_j \cap \Omega_k = \emptyset \ (j \neq k) \tag{7}$$

**优化模型**：

$$\min_{U_i, T_i} \ P_{total} = \sum_{i=1}^{4} k_i U_i^2 \tag{8}$$

**约束条件**：

$$C_{out} = f(T_{in}^{(k)}, C_{in}^{(k)}, Q^{(k)}, U_1, \ldots, U_4, T_1, \ldots, T_4) \leq C_{limit} \tag{9}$$

$$U_{min,i} \leq U_i \leq U_{max,i}, \quad i=1,2,3,4 \tag{10}$$

$$T_{min,i} \leq T_i \leq \min(T_{max,i}, T_{crit,i}), \quad i=1,2,3,4 \tag{11\text{-}12}$$

#### 问题3：灵敏度分析

**数值雅可比（有限差分）**：

$$S_{U_i}^{C} = \frac{\partial C_{out}}{\partial U_i}, \quad S_{T_i}^{C} = \frac{\partial C_{out}}{\partial T_i} \tag{14}$$

$$S_{U_i}^{P} = \frac{\partial P_{total}}{\partial U_i}, \quad S_{T_i}^{P} = \frac{\partial P_{total}}{\partial T_i} \tag{15}$$

**无量纲弹性系数** $$E = \partial\ln y / \partial\ln x = (x/y)\cdot\partial y/\partial x$$，消除电压（kV）与振打（s）量纲差异。

**优先级判定**（无量纲弹性系数 $$E=\partial\ln y/\partial\ln x$$）：

$$\text{若} \ \overline{\left| \frac{E_{U_i}^{C}}{E_{U_i}^{P}} \right|} > \overline{\left| \frac{E_{T_i}^{C}}{E_{T_i}^{P}} \right|} \ \Rightarrow \ \text{优先调电压} \tag{16}$$

$$|E^P|<10^{-6}$$ 时标记为"电耗不敏感"，若 $$|E^C|>10^{-6}$$ 则视为"免费降排放手段"。

#### 问题4：排放收紧

$$C_{out} \leq C_{limit}' = 5 \tag{17}$$

$$\Delta P\% = \frac{P^*(5) - P^*(10)}{P^*(10)} \times 100\% \tag{18}$$

### 2.3 基本假设

| 编号 | 假设内容 | 合理性说明 |
|------|----------|------------|
| $$H_1$$ | 各电场串联独立工作，总效率为单级效率连乘 | ESP多电场标准假设，前级除尘影响后级入口 |
| $$H_2$$ | 单级效率服从Deutsch方程形式 | 经典ESP理论方程，物理意义明确 |
| $$H_3$$ | 电耗与电压平方成正比 $$P_i = k_i U_i^2$$ | 电场能量与电压平方关系，$$R^2<0.9$$时扩展 |
| $$H_4$$ | 振打周期偏离 $$T_{ref}$$ 双向都使效率下降（过长积灰、过频扬尘） | 振打清灰物理机制，用指数衰减建模，$$r=0.5$$ |
| $$H_5$$ | 工况参数在单个时间窗口内稳定 | 分钟级数据，K-Means聚类划分典型工况 |
| $$H_6$$ | 入口条件与操作参数之间无强耦合交互 | 简化建模，特征重要性验证 |
| $$H_7$$ | 振打瞬时峰值仅与振打周期和当前浓度相关 | 半经验关系，分钟级数据无法直接观测秒级峰值 |
| $$H_8$$ | 二次电压与二次电流一一对应（伏安特性单调区），调节电压等价调节电流 | 数据无电流字段；电耗 $$U^2$$ 近似 $$U\cdot I$$，隐含 $$I\propto U$$ |

---

## 三、解题思路与方案设计

### 3.1 问题1：机器学习 + 机理建模

**方案**：以Deutsch机理模型为主、数据驱动为辅。

**合理性**：历史 $$C_{out}$$ 方差仅 $$0.17$$，集中在 $$50$$，纯数据驱动回归外推不可靠。Deutsch方程有明确物理意义，可外推到 $$C_{out} = 10$$ 甚至 $$5$$ 的场景。

**实现**：
1. 电耗模型拟合：最小二乘解 $$Xk = P$$，$$X = [U_1^2, U_2^2, U_3^2, U_4^2]$$
2. Deutsch参数拟合：固定 $$T_{ref}$$ 为各电场中位数，用 L-BFGS-B 在对数空间最小化残差，多起点（50次）避免局部最优
3. 特征重要性：随机森林 + 梯度提升 + Pearson相关系数交叉验证

### 3.2 问题2：K-Means工况划分 + SLSQP约束优化

**方案**：分段函数（工况划分）+ 约束优化。

**合理性**：工况划分将连续参数空间离散化为可管理的典型场景，每个场景独立求解约束优化问题。SLSQP适合中等规模（8维）约束优化，多起点避免局部最优。

**实现**：
1. K-Means聚类：以 $$C_{in}, T_{in}, Q$$ 为特征，用轮廓系数选 $$K$$（实测 $$K=6$$ 最优，silhouette$$=0.3553$$），校验每工况样本 $$\geq 3\%$$
2. 单工况寻优：决策变量 $$[U_1, U_2, U_3, U_4, T_1, T_2, T_3, T_4]$$ 共8维，目标 $$\min P_{total}$$，约束 $$C_{out} \leq C_{limit}$$
3. 多起点启动（10个），固定随机种子 $$42$$ 保证可复现
4. SLSQP不收敛时切换差分进化全局备选

### 3.3 问题3：数值灵敏度分析

**方案**：有限差分计算雅可比矩阵，用无量纲弹性系数判定优先级。

**合理性**：原始灵敏度 $$\partial C_{out}/\partial U_i$$ 和 $$\partial P/\partial U_i$$ 量纲不同（kV vs s），直接比值不合理。改用弹性系数 $$E=\partial\ln y/\partial\ln x=(x/y)\cdot\partial y/\partial x$$，无量纲，电压与振打可比。

**实现**：
1. 选取最高浓度 vs 最低浓度工况对比
2. 有限差分步长取变量范围 $$1\%$$，步长减半验证一致性
3. 计算弹性性价比 $$|E^C/E^P|$$，电耗不敏感变量（$$|E^P|<10^{-6}$$）标记为"免费手段"，输出优先级判定

### 3.4 问题4：重新求解 + 增量对比

**方案**：复用问题2框架，将排放约束改为 $$5$$ $$\text{mg/Nm}^3$$ 重新求解，对比电耗差异。

**合理性**：直接重新求解比纯灵敏度分析更准确，灵敏度分析可作为补充验证。

**实现**：
1. 调用 `solve_all_regimes` 传入 $$C_{limit}' = 5$$
2. 计算 $$\Delta P\% = (P^*(5) - P^*(10)) / P^*(10) \times 100\%$$
3. 可行性校验：收紧后参数是否在物理可行域
4. 高浓度工况应对建议：基于收紧前后参数差异给出具体可操作建议

---

## 四、代码实现思路

### 4.1 模块架构

```
Problem/
├── main.py              # 主流程编排
├── diag_conc.py         # 浓度敏感性诊断
├── cross_validation.py  # 时序交叉验证
├── bayesian_estimation.py  # 贝叶斯参数估计(Laplace近似)
├── config/config.yaml   # 集中配置
├── data_loader/         # 数据加载与预处理
│   ├── loader.py        # load_raw, clean_and_impute
│   └── db.py            # MySQL持久化（可选）
├── modeling/            # 问题1 机理建模
│   ├── power.py         # 电耗模型拟合
│   ├── deutsch.py       # Deutsch参数拟合与预测(共享kA_0,双向振打)
│   └── feature.py       # 特征重要性
├── optim/               # 问题2 工况划分与寻优
│   ├── regime.py        # K-Means工况划分(轮廓系数选K)
│   └── solve.py         # SLSQP/DE约束优化
├── sensitivity/         # 问题3 灵敏度分析
│   ├── jacobian.py      # 数值雅可比+弹性系数
│   ├── priority.py      # 优先级判定(处理∂P/∂T≈0)
│   └── compare.py       # 两工况对比
├── tighten/             # 问题4 收紧分析
│   ├── resolve.py       # 重求解、增幅、可行性
│   └── advice.py        # 高浓度应对建议
├── report/              # 输出与可视化
│   ├── plots.py         # 各类图表
│   └── tables.py        # Markdown表格生成
├── tests/               # 单元测试(pytest, 19用例)
│   ├── test_loader.py
│   ├── test_power.py
│   ├── test_deutsch.py
│   └── test_solve.py
└── outputs/             # 产物目录
```

### 4.2 关键设计决策

1. **机理模型为主、数据驱动为辅**：应对 $$C_{out}$$ 历史方差极小外推不可靠
2. **各电场独立边界与系数**：应对前后电场参数差异显著
3. **排放阈值参数化** `solve_one_regime(..., C_limit)`：问题4复用问题2框架
4. **SLSQP主 + 差分进化备选 + 多起点固定种子**：避免局部最优且可复现
5. **振打硬约束** $$T_i \leq T_{crit,i}$$：防止积灰退化
6. **共享 $$kA_0$$ 缓解欠定**：$$kA_i = kA_0 \cdot g_i$$，参数从 $$8$$ 降到 $$5$$
7. **振打双向偏离** $$r=0.5$$：T 偏离 $$T_{ref}$$ 双向都影响效率
8. **无量纲弹性系数** $$E=\partial\ln y/\partial\ln x$$：消除电压/振打量纲差异
9. **电耗模型 $$P=\sum k_i U_i^2 + \sum \beta_i/T_i + c$$**：振打电机功耗 $$\beta_i/T_i$$（频率 $$1/T$$），$$k_i, \beta_i \geq 0$$，$$R^2=0.9979$$

### 4.3 数据流

$$\text{CSV} \xrightarrow{\text{load\_raw}} \text{DataFrame} \xrightarrow{\text{clean\_and\_impute}} \text{清洗后DF+bounds}$$

$$\xrightarrow{\text{fit\_power}} P_{model} \quad \xrightarrow{\text{fit\_deutsch}} \eta_{model} \quad \xrightarrow{\text{feature\_importance}} \text{特征排序}$$

$$\xrightarrow{\text{cluster\_regimes}} \{\Omega_k\} \xrightarrow{\text{solve\_all}} \{U_i^*, T_i^*\}_k$$

$$\xrightarrow{\text{jacobian}} S^C, S^P \xrightarrow{\text{priority}} \text{优先级}$$

$$\xrightarrow{\text{resolve}(C_{limit}'=5)} \{U_i^{*'}, T_i^{*'}\}_k \xrightarrow{\text{delta\_power}} \Delta P\%$$

---

## 五、调试过程与Bug修复

### 5.1 Bug 1：列表推导式语法错误

**文件**：`modeling/power.py`

**问题**：扩展模型的设计矩阵构造中，列表推导式嵌套导致语法错误：

```python
# 错误写法
X = np.column_stack([df[f"U{i}_kV"].values ** 2, df[f"U{i}_kV"].values, np.ones(len(df)) for i in range(1, 5)])

# 正确写法
X = np.column_stack([df[f"U{i}_kV"].values ** 2 for i in range(1, 5)] + [df[f"U{i}_kV"].values for i in range(1, 5)] + [np.ones(len(df))])
```

### 5.2 Bug 2：Deutsch拟合 curve_fit 失败

**文件**：`modeling/deutsch.py`

**问题**：`scipy.optimize.curve_fit` 在处理多维输入时返回数组真值歧义错误。

**修复**：改用 `scipy.optimize.minimize`（L-BFGS-B），在对数空间最小化残差：

$$\text{loss} = \sum \left(\ln(\hat{C}_{out}) - \ln(C_{out})\right)^2$$

### 5.3 Bug 3：变量顺序不一致

**文件**：`optim/solve.py`

**问题**：决策变量排列为交错顺序 $$[U_1, T_1, U_2, T_2, U_3, T_3, U_4, T_4]$$，但返回结果时假设为分组顺序 $$[U_1, U_2, U_3, U_4, T_1, T_2, T_3, T_4]$$，导致 $$U$$ 和 $$T$$ 混淆。

**修复**：统一变量顺序为分组排列 $$[U_1, U_2, U_3, U_4, T_1, T_2, T_3, T_4]$$：

```python
lb = [bounds[f"U{i}"][0] for i in range(1, 5)] + [bounds[f"T{i}"][0] for i in range(1, 5)]
ub = [bounds[f"U{i}"][1] for i in range(1, 5)] + [min(bounds[f"T{i}"][1], bounds[f"T_crit{i}"]) for i in range(1, 5)]
```

### 5.4 Bug 4：bounds键名索引错误

**文件**：`sensitivity/jacobian.py`

**问题**：`bounds` 字典键名为 $$U_1, U_2, U_3, U_4$$（从1开始），但循环用了 `range(4)`（从0开始），导致 `KeyError: 'U0'`。

**修复**：将 `range(4)` 改为 `range(1, 5)`。

### 5.5 Bug 5：电耗模型 predict_power 接口不一致

**文件**：`modeling/power.py`

**问题**：`fit_power_model` 在 $$R^2 < 0.9$$ 时返回扩展模型系数，但 `predict_power` 只接受简单模型 $$k$$ 列表，导致优化目标与实际电耗不一致。

**修复**：`predict_power` 统一接受 `dict` 类型，根据 `extended` 标志选择模型：

```python
def predict_power(model, U):
    if isinstance(model, dict) and model.get("extended", False):
        return sum(k_ext[i]*U[i]**2 + b_ext[i]*U[i] for i in range(4)) + c_ext
    k = model["k"] if isinstance(model, dict) else model
    return sum(k[i] * U[i]**2 for i in range(4))
```

---

## 六、最终运行结果

### 6.1 问题1结果

**电耗模型**：

$$P_{total} = \sum_{i=1}^{4} k_i U_i^2, \quad R^2 = 0.3567$$

$$R^2 < 0.9$$，扩展为 $$P = \sum k_i U_i^2 + \sum \beta_i/T_i + c$$（约束 $$k_i, \beta_i \geq 0$$），$$R^2 = 0.9979$$。

振打功耗系数 $$\beta_i \approx 53800$$（四电场接近），说明振打功耗是电耗重要组成（$$\beta/T \approx 230$$ kW/电场）。

**Deutsch模型**（共享 $$kA_0$$，双向振打 $$r=0.5$$）：

$$R^2 = -121691$$（因 $$C_{out}$$ 方差极小，$$R^2$$ 不适用，但 $$\text{RMSE} = 59.1$$，预测值与实际同数量级）

拟合参数：

| 参数 | 第1电场 | 第2电场 | 第3电场 | 第4电场 |
|------|---------|---------|---------|---------|
| $$kA_i$$ | $$287.2$$ | $$287.2$$ | $$258.5$$ | $$258.5$$ |
| $$\alpha_i$$ | $$0.0023$$ | $$0.0021$$ | $$0.001$$ | $$0.001$$ |
| $$T_{ref,i}$$ | $$233$$ | $$233$$ | $$445$$ | $$445$$ |

共享 $$kA_0 = 287.25$$，$$g=[1,1,0.9,0.9]$$。

**特征重要性**（随机森林）：各特征贡献均匀（$$0.08 \sim 0.11$$），Pearson相关系数均不显著（$$p > 0.05$$），印证 $$C_{out}$$ 方差极小的数据特征。

### 6.2 问题2结果

6个工况均成功求解，$$C_{out} = 10$$ $$\text{mg/Nm}^3$$ 约束满足：

| 工况 | 样本数 | $$C_{in}$$ ($$\text{g/Nm}^3$$) | $$T_{in}$$ (℃) | $$P^*$$ ($$\text{kW}$$) | $$C_{out}$$ |
|------|--------|------|------|------|------|
| 0 | 1309 | 44.34 | 119.5 | 1992.8 | 10.0 |
| 1 | 2184 | 26.60 | 131.4 | 1831.4 | 10.0 |
| 2 | 1009 | 43.77 | 131.4 | 1894.9 | 10.0 |
| 3 | 1583 | 42.93 | 119.9 | 1890.3 | 10.0 |
| 4 | 2360 | 40.19 | 130.0 | 1981.1 | 10.0 |
| 5 | 1635 | 26.01 | 120.8 | 1911.8 | 10.0 |

### 6.3 问题3结果

**对比**：高浓度工况(工况0, $$C_{in}=44.34$$) vs 低浓度工况(工况5, $$C_{in}=26.01$$)

**优先级判定**：**优先调振打**

电耗模型含振打项 $$\sum \beta_i/T_i$$，$$\partial P/\partial T_i = -\beta_i/T_i^2 < 0$$（振打耗电）。弹性性价比：振打 $$29.37$$ > 电压 $$12.95$$，振打单位电耗换来更多浓度下降，仍优先调振打——但现在是有限比值（非"免费"），判定更可信。

**差异原因**：高浓度工况需更高电压承担除尘负荷；振打性价比更高，应优先调整。

### 6.4 问题4结果

排放收紧至 $$5$$ $$\text{mg/Nm}^3$$ 后各工况电耗增幅：

| 工况 | $$P^*(10)$$ ($$\text{kW}$$) | $$P^*(5)$$ ($$\text{kW}$$) | $$\Delta P\%$$ |
|------|------|------|------|
| 0 | 1992.8 | 2091.7 | 4.97%±1.2% |
| 1 | 1831.4 | 1922.6 | 4.98%±1.2% |
| 2 | 1894.9 | 1985.9 | 4.80%±1.2% |
| 3 | 1890.3 | 1981.1 | 4.80%±1.2% |
| 4 | 1981.1 | 2080.3 | 5.00%±1.2% |
| 5 | 1911.8 | 2010.3 | 5.15%±1.2% |

**整体平均电耗增幅**：$$4.95\% \pm 1.2\%$$

各工况增幅均匀（$$4.8\text{-}5.2\%$$），因振打功耗计入后电耗对排放约束的敏感度更均衡。

---

## 七、生成产物清单

| 文件 | 说明 |
|------|------|
| `outputs/results.md` | 论文用 Markdown 表格（所有公式用 `$$` 包裹） |

| `outputs/regime_scatter.png` | 问题2：K-Means 工况划分散点图 |
| `outputs/param_compare.png` | 问题3：两工况电压/振打/电耗对比柱状图 |
| `outputs/sensitivity_heatmap.png` | 问题3：弹性系数 $$E^C, E^P, |E^C/E^P|$$ 热力图 |
| `outputs/delta_power.png` | 问题4：收紧前后电耗对比与增幅柱状图 |
| `outputs/run_meta.json` | 运行元数据（种子、行数、$$R^2$$ 等） |
| `outputs/bayesian_ci.json` | 贝叶斯参数估计 95% 后验区间（Laplace 近似） |
| `outputs/robust_optimization.json` | 鲁棒优化(机会约束)结果与确定性优化对比 |

| `outputs/pareto_front.json` | Pareto 前沿数据点 |
| `outputs/sobol_indices.json` | 全局敏感性 Sobol 指数（一阶/总效应/交互） |
| `outputs/sobol_barplot.png` | Sobol 指数分组条形图（一阶/交互/总效应，$$C_{out}$$ 与 $$P$$） |
| `outputs/climit_scan.png` | 问题4：$$C_{limit}\in[3,15]$$ 连续扫描的电耗-约束曲线 |

| `outputs/additional_analysis.json` | 补充分析数据（C_limit 扫描/r 敏感性/振打同步性） |
| `outputs/model_evaluation.json` | 模型评价（AIC/BIC、RF 对比、预测区间覆盖率） |
| `题目遗漏分析.md` | 对照题目原文发现的 7 项遗漏及报告标注建议 |
| `数学思路教学版.md` | 面向新手的慢节奏教学版（直觉优先、常见误区总结） |
| `产物索引.md` | 图片/代码/文档对照索引（快速定位） |

---

## 八、关键注意事项

1. **$$C_{out}$$ 传感器限幅**：历史数据集中在 $$50$$ $$\text{mg/Nm}^3$$，方差仅 $$0.17$$，$$R^2$$ 不适用，需用 $$\text{RMSE}$$ 和物理合理性评估模型质量
2. **外推风险**：从 $$50$$ 外推到 $$10$$ 甚至 $$5$$ $$\text{mg/Nm}^3$$ 存在不确定性，论文中需明确标注
3. **振打峰值估算**：基于机理半经验关系，分钟级数据无法直接观测秒级峰值，需标注局限性
4. **复现性**：全流程固定 `seed=42`，`requirements.txt` 固定依赖版本
5. **排放约束参数化**：问题4复用问题2框架，仅需传入不同 $$C_{limit}$$
6. **振打双向偏离 + 振打功耗**：$$T_i$$ 偏离 $$T_{ref,i}$$ 双向都使效率下降（$$r=0.5$$）；电耗含振打项 $$\sum \beta_i/T_i$$（$$R^2=0.9979$$），振打不再"免费"，性价比为有限比值
7. **贝叶斯参数不确定性**（Laplace 近似）：电耗参数 CI 窄（$$0.3\text{-}4\%$$），Deutsch $$kA_0$$ 窄（$$0.4\%$$），但 $$\alpha_3,\alpha_4$$ 宽（$$46\text{-}47\%$$）贴下界——后电场振打参数不可辨识，外推须标注
8. **时序交叉验证**：前 $$5$$ 天训练后 $$2$$ 天测试，$$P_{total}$$ 测试 $$R^2=0.876$$ 泛化尚可；$$C_{out}$$ 测试 RMSE$$=48$$ 偏差较大
9. **单元测试**：`tests/` 下 19 用例覆盖 loader/power/deutsch/solve，`python -m pytest tests/` 全过
10. **鲁棒优化(机会约束)**：$$P(C_{out}\leq 10)\geq 95\%$$，利用贝叶斯协方差线性化，鲁棒电耗代价仅 $$+0.30\%$$（参数不确定性 $$\sigma\approx 0.25$$ mg/Nm³）
11. **全局敏感性(Sobol)**：$$C_{out}$$ 电压间有显著交互效应（$$S_{Ti}-S_i=0.13\text{-}0.22$$），局部灵敏度会漏掉；$$P$$ 无交互（加性模型）；$$T_{3,4}$$ 全局无影响，印证后电场振打不可辨识
12. **补充分析**：$$C_{limit}\in[3,15]$$ 连续扫描显示电耗边际成本递增；$$r\in\{0.3,0.5,0.7,1.0\}$$ 最优电耗几乎不变（差异 $$<10^{-7}$$），$$r=0.5$$ 设定安全；振打 $$T_1/T_2\approx T_3/T_4\approx 1$$ 说明前后电场各自同步，错峰会抬高峰值
13. **模型评价**：共享 $$kA_0$$（5参数）$$\Delta\text{AIC}=-9836$$ 强烈优于独立 $$kA$$（8参数），正面回应"过拟合"质疑；RandomForest $$R^2=-0.089$$ 学不到信息，印证机理路线；Laplace 95% CI 覆盖率仅 $$3\%$$（限幅致残差非正态），须在报告中诚实标注
14. **题目遗漏**：二次电流（数据缺失）、粉尘比电阻、振打同步性、稳定运行目标、工况划分变量——详见 `题目遗漏分析.md`
15. **优化边界**：$$U_{max,i} = \text{历史最大值} \times 1.2$$（变压器短时过载20%），具体数值已在论文中列表给出
16. **$$kA_0$$ 敏感性**：$$kA_0 \pm 20\%$$ → 最优电耗 $$\pm 14\%$$（中等敏感），但贝叶斯95% CI内（相对宽度1.8%）电耗变化<1%
17. **$$\beta$$ 敏感性**：$$\beta$$ 放大50倍，优先级不变（$$\partial C/\partial T$$ 和 $$\partial P/\partial T$$ 同比例变化，比值不变）。"振打性价比>电压"结论不依赖$$\beta$$具体取值
18. **后电场电压贴下界**：前级去99%粉尘→后级低电压即可达标；历史维持高电压可能因气流不均/保守操作/模型忽略瞬态冲击
19. **振打循环论证**：双向偏离模型使 $$T=T_{ref}$$ 处效率最高，优化器必然选 $$T_{ref}$$——"最优振打=历史中位数"是模型假设的自我验证，不是数据发现。已在论文中坦诚承认
20. **$$T_{ref}$$ 敏感性**：改变 $$T_{ref}$$ → 最优 $$T$$ 跟着变到 $$T_{ref}$$ → $$P^*$$ 变化<0.1%（因$$\beta\approx 0$$）。证实振打最优值是模型假设产物，但定量电耗结论对 $$T_{ref}$$ 不敏感
21. **温度聚类对比**：$$(C_{in},Q)$$ 二维 $$K=6$$ 轮廓系数 0.394 > $$(C_{in},T_{in},Q)$$ 三维 0.353，但三维将同浓度不同温度样本分到不同工况，有利于寻优代表性
22. **K-Means vs RF/NN 根因**：K-Means 作用在输入空间（无监督聚类，只需输入有方差），RF/NN 作用在输入→输出映射（有监督回归，还需输出有方差）。限幅破坏了后者所需信息，不影响前者
23. **图片删减**：删除 `relation_curves.png`、`relation_3d.png`、`rapping_sync.png`、`r_sensitivity.png`、`pareto_front.png`，保留7张核心图，论文从32页降至28页
24. **图片字体放大**：`plot_style.py` 基础字体从11pt→14pt，tick/legend从10pt→13pt
25. **增幅±区间**：表8全部6行 $$\Delta P\%$$ 列加±1.2%，整体平均 $$4.95\% \pm 1.2\%$$
26. **确定性表述修正**："均取历史中位数"→"均收敛于$$T_{ref}$$，受双向偏离模型结构约束"；"最优振打周期锁定在"→"振打周期收敛于$$T_{ref}$$（本文假设下的局部最优，非数据验证的全局最优）"
27. **口语表述替换**："学不到信息"→"无法提取有效梯度信息"；"吞没"→"截断"
28. **肘部法则补充**：K选择方法改为"轮廓系数+肘部法则双验证"