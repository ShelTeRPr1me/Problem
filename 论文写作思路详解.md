# 论文写作思路详解

> 本文档逐节阐述论文的写作逻辑：**为什么这样写、什么问题驱动了这样的写法、每一步的因果链是什么**。目标是让读者理解论文不是"写出来的"，而是"问题推出来的"。

---

## 一、整体架构：问题递进决定章节递进

### 1.1 为什么是"建模→优化→解释→外推"四步？

**问题根源**：题目四个问题本身就有递进关系——

| 问题 | 本质 | 输出 | 下一个问题依赖它什么？ |
|------|------|------|------------------------|
| 问题1 | 因果建模 | $C_{out}=f(U,T,C_{in},T_{in},Q)$ 和 $P=g(U,T)$ | 问题2需要这两个函数做目标+约束 |
| 问题2 | 约束优化 | 各工况最优 $(U^*,T^*)$ | 问题3需要对比两个工况的解 |
| 问题3 | 灵敏度解释 | 优先级规则 | 问题4不需要，但问题3验证了模型的"可解释性" |
| 问题4 | 政策外推 | $\Delta P\%$ | 独立于问题3，但复用问题2的优化框架 |

**写作决策**：论文章节严格按此递进排列。不按"模型→算法→实验"的传统AI论文写法，因为本题不是算法创新，而是**工程建模+优化求解**，逻辑链必须跟着问题走。

### 1.2 为什么符号说明放在假设之前？

**问题**：读者第一次遇到 $kA_0$、$\beta_i$、$T_{ref,i}$ 等符号是在哪里？如果假设先出现，读者看到"假设 $H_4$：振打周期偏离 $T_{ref,i}$"时会问"$T_{ref,i}$ 是什么？"

**决策**：先给符号表，让读者有"字典"可查，再读假设和模型公式。这是数学建模国赛的常见惯例（CUMCM 模板推荐顺序：问题重述→假设→符号→模型），但我们的微调是**符号→假设**，因为假设中引用了大量自定义符号，先定义符号更顺。

---

## 二、摘要：四个问题的"一句话结论"写法

### 2.1 为什么每个问题用"首先…接着…最后…结果表明…"的固定句式？

**问题**：评委在30秒内判断论文水平，摘要必须让每个问题的**方法+结论**一目了然。

**决策**：每个问题用三段式：
1. **首先**：做了什么（方法一句话概括）
2. **接着/最后**：怎么做（关键技术点，如"L-BFGS-B多起点50次"）
3. **结果表明**：核心数值结论（如"$R^2=0.9979$"）

### 2.2 摘要中为什么强调 $R^2=0.9979$ 而不是 $0.9349$？

**问题**：旧版电耗模型 $P=\sum k_iU_i^2+c$ 的 $R^2=0.9349$，新版加入振打项后 $R^2=0.9979$。哪个该写进摘要？

**决策**：写 0.9979。原因：
- 摘要中的数值必须与正文最终模型一致
- 0.9979 说明振打功耗不可忽略，这是本文的一个**发现**（不是所有论文都意识到振打耗电）
- 0.9349 是中间结果（无振打项模型），在正文中作为对比出现，但摘要只写最终值

### 2.3 为什么问题3结论从"优先调电压"改为"优先调振打"？

**问题链**：
1. 旧模型 $P=\sum k_iU_i^2+c$ 不含振打 → $\partial P/\partial T=0$ → 振打"免费" → 性价比=∞ → 结论"优先调振打"（但这是模型缺陷导致的虚假结论）
2. 旧模型振打单向衰减 → 最优解 $T_i \leq T_{ref,i}$ → 衰减项不激活 → $\partial C_{out}/\partial T \approx 0$ → 振打性价比=0 → 结论"优先调电压"（也是虚假的）
3. **修复后**：振打功耗项使 $\partial P/\partial T \neq 0$，双向偏离使 $\partial C_{out}/\partial T \neq 0$，两个偏导都是有限非零值 → 性价比=29.37（振打）> 12.95（电压）→ **优先调振打**

**关键认知**：结论不是"偏好"，而是**模型结构的逻辑推论**。模型改了，结论必须跟着改。旧结论的两个版本都是模型缺陷的产物，不是物理真相。

---

## 三、问题1：为什么"机理为主、数据驱动为辅"？

### 3.1 $C_{out}$ 限幅问题如何驱动建模路线？

**发现**：$C_{out}$ 的 54.6% 数据精确等于 50.00，标准差仅 0.17。

**推理链**：
1. 如果是自然波动，不可能 54.6% 精确卡在同一个值 → **传感器量程限幅**
2. 限幅意味着 $C_{out} \in [49.5, 50.0]$ 区间内的信息被"压扁"了
3. 纯数据驱动（随机森林、梯度提升）在这个区间内学不到"调 $U$ 如何改变 $C_{out}$"的关系 → RF 的 $R^2=-0.089$（比均值还差）
4. 但物理上 $C_{out}$ 确实随 $U$ 变化（Deutsch 方程保证单调性），只是传感器看不到
5. **结论**：必须用 Deutsch 方程等物理机理外推，数据只用来拟合参数

**写作策略**：在问题1开头就亮明这个判断，让评委理解后续所有建模选择的根因。不把这个说清楚，评委会对"为什么不用深度学习"产生疑问。

### 3.2 为什么电耗模型要加振打项 $\beta_i/T_i$？

**问题链**：
1. 纯物理推导 $P_i = k_i U_i^2$（电场能量∝电压²）→ 拟合 $R^2=0.3567$，太差
2. 加常数项 $c$（空载损耗）→ $R^2=0.9349$，尚可
3. 但残差图显示残差与 $T_i$ 有系统相关性 → **振打电机功耗被遗漏**
4. 物理上振打电机功率∝振打频率=1/T → 加入 $\beta_i/T_i$ → $R^2=0.9979$

**为什么这一步至关重要**：如果不加振打项，问题3的灵敏度分析中 $\partial P/\partial T=0$，振打性价比要么=∞（如果 $\partial C/\partial T \neq 0$）要么=0/0 不定（如果 $\partial C/\partial T=0$），两种情况都给出虚假结论。**振打项的加入不是为了提高 R²，而是为了让问题3的灵敏度分析有物理意义。**

### 3.3 为什么振打衰减从单向改为双向？

**问题链**：
1. 单向模型 $\eta(T) = \eta_0 \cdot \exp(-\alpha \cdot \max(0, T-T_{ref}))$：$T < T_{ref}$ 时衰减=1（效率不降）
2. 优化器发现：减小 $T$ 既不降效率又不增电耗（旧模型）→ 把 $T$ 压到下界
3. 这不符合物理：振打过频→二次扬尘增加→效率也降
4. **双向偏离**：$d = \max(0, T-T_{ref}) + r \cdot \max(0, T_{ref}-T)$，两个方向偏离都降效率
5. 优化器在 $T_{ref}$ 附近寻优（效率最高点），不再压到下界

**为什么 $r=0.5$ 而不是从数据拟合？** 因为 $C_{out}$ 限幅，数据中 $T < T_{ref}$ 的工况很少且 $C_{out}$ 都卡在 50，$r$ 不可辨识。取 $r=0.5$ 是工程先验（过频振打的副作用约为过疏的一半），且 $r$ 敏感性分析表明 $r \in [0.3, 1.0]$ 对最优电耗几乎无影响。

---

## 四、问题2：工况划分与约束优化

### 4.1 为什么用 K-Means 而不是其他聚类？

**问题**：工况划分的方法选择——GMM？DBSCAN？K-Means？

**决策逻辑**：
1. 工况是入口条件 $(C_{in}, T_{in}, Q)$ 的"团状"分组，不是任意形状 → 排除 DBSCAN（擅长噪声+任意形状）
2. 工况间不需要概率归属 → GMM 的软分配优势用不上，且 GMM 对初始化敏感
3. K-Means 简单、可解释、与"典型工况=聚类中心"的物理直觉一致 → **选择 K-Means**

### 4.2 为什么 K=6 用轮廓系数确定？

**问题**：K 怎么选？

**推理**：
1. K 太小→工况内异质性大，最优参数不具代表性
2. K 太大→某些工况样本太少，统计不可靠
3. 轮廓系数 $s_i \in [-1, 1]$ 衡量"簇内紧凑 vs 簇间分离"，是 K 选择的标准无监督指标
4. 约束：每工况占比 ≥ 3%（防止出现"微型工况"）
5. 实测：K=6 时 silhouette=0.3553（最大），K=7 出现 <3% 的工况 → **K=6**

### 4.3 为什么优化目标只写 $\min P$ 而不是三目标？

**问题**：题目说"低排放、低电耗、稳定运行"三目标，为什么只优化 $\min P$？

**回答**（这就是"稳定运行讨论"的核心）：

1. **低排放** → 用硬约束 $C_{out} \leq C_{limit}$ 处理，不放入目标。原因：排放是"必须满足"的法规要求，不是"越低越好"的偏好。实际运行中 $C_{out}=10$ 和 $C_{out}=5$ 对企业来说差别不大（都达标），但电耗差别显著。
2. **低电耗** → 放入目标 $\min P$。原因：电耗是企业直接关心的运营成本。
3. **稳定运行** → 不显式放入目标或约束，而是通过模型结构**隐式保障**。具体三条保障机制：

   **机制1：双向偏离模型锁定振打在 $T_{ref}$ 附近**
   - 单向模型下优化器把 $T$ 压到下界→振打过于频繁→机械疲劳+二次扬尘→不稳定
   - 双向偏离模型下 $T_{ref}$ 是效率最高点→优化器自然选 $T \approx T_{ref}$→运行平稳
   - **特征驱动**：旧模型给出物理不合理的振打策略→说明模型缺少"过频也有害"的机制→引入双向偏离

   **机制2：鲁棒优化验证稳定裕度**
   - 机会约束 $\Pr(C_{out} \leq C_{limit}) \geq 95\%$ 下，参数不确定性 $\sigma \approx 0.25$ mg/Nm³
   - 电耗代价仅 +0.30%→最优解对扰动不敏感→稳定裕度充足
   - **特征驱动**：如果鲁棒代价很大（比如 +20%），说明当前解在约束边界附近"踩线"，扰动容易越界→不稳定。但实际代价极小→稳定。

   **机制3：Pareto 前沿表明低排放与低峰值不冲突**
   - $C_{peak}$ 降 80% 仅需 +0.2% 电耗→三目标权衡空间宽裕
   - **特征驱动**：如果 Pareto 前沿很"弯"（降峰值需大幅增电耗），则三目标有实质冲突，需要显式多目标。但实际几乎不冲突→单目标足够。

**写作策略**：在问题4末尾用一段"稳定运行讨论"集中回应，而不是在问题2中展开。原因：
- 问题2的任务是"求解最优参数"，不是"讨论稳定运行"
- 稳定运行的论证需要引用鲁棒优化和 Pareto 前沿的结果，这些在问题2阶段还没计算
- 放在问题4末尾逻辑最顺：问题4已经做了收紧分析，顺势讨论"更严约束下是否还稳定"

---

## 五、问题3：灵敏度与优先级

### 5.1 为什么用弹性系数而不是原始偏导？

**问题**：电压偏导 $\partial C/\partial U$ 单位是 mg/(Nm³·kV)，振打偏导 $\partial C/\partial T$ 单位是 mg/(Nm³·s)，量纲不同，无法直接比较。

**决策**：弹性系数 $E = \partial \ln y / \partial \ln x = (x/y) \cdot \partial y / \partial x$，无量纲。电压和振打的弹性系数可以直接比较和平均。

**DeepSeek 批评的回应**：DeepSeek 指出"原始灵敏度量纲不统一"是对的，我们已经修复。但 DeepSeek 说"R² 不可能为负这么大"是错的——$R^2 = 1 - SS_{res}/SS_{tot}$，当模型预测比均值还差时 $R^2$ 可以为任意大的负数。

### 5.2 为什么补充条件性优先级规则？

**问题**：原论文只给了一个结论"优先调振打"，但题目要求"优先级**变化规律**"——暗示优先级可能随工况变化。

**做法**：对全部 6 个工况分别计算性价比，发现所有工况均为"优先调振打"（振打性价比约为电压的 2.2~2.8 倍），结论不随工况反转。

**写作策略**：即使结论不变，也要展示"我们检查了所有工况"的过程，否则评委会质疑"你只算了一个工况就下结论？"。条件性规则表的价值在于**展示稳健性**，而非发现反转。

---

## 六、问题4：排放收紧影响

### 6.1 为什么电耗增幅从 6.90% 变为 4.95%？

**根因**：旧模型 $P=\sum k_iU_i^2+c$ 中振打不耗电，收紧排放时只能靠提电压→电耗增幅大。新模型 $P=\sum k_iU_i^2+\sum\beta_i/T_i+c$ 中振打也耗电，收紧时可以同时调振打和电压→电耗增幅小。

**物理含义**：振打是"另一个调节旋钮"，多一个自由度意味着达到同样排放目标所需的总能量增量更小。

### 6.2 为什么各工况增幅差异不大（极差仅 0.35%）？

**旧模型**下高浓度工况增幅 7.8%、低浓度 4.3%，差异大——因为高浓度工况"踩线"更紧，收紧后必须大幅提电压。

**新模型**下各工况增幅都在 4.80%~5.15%——因为振打提供了额外的调节自由度，即使高浓度工况也不必只靠提电压，可以通过微调振打分担一部分减排任务，使得各工况的边际代价趋于均匀。

---

## 七、第7/8节拆分逻辑

### 7.1 为什么把"优点"和"缺点"拆成独立文件？

**问题**：写作时经常出现"优点写了4条，缺点只写了1条"的不平衡。拆分后可以独立审视：
- 优点文件：只关注"这个模型做对了什么"
- 缺点文件：只关注"这个模型做错了/做不到什么"

**更重要的是**：缺点列表必须是**当前模型的真实局限**，不能把已经修复的问题还列在缺点里。旧版缺点中的"振打模型单向局限"已经在正文中改为双向偏离，再列在缺点里就是自相矛盾。拆分后更容易逐条检查"这条缺点是否仍然成立"。

### 7.2 为什么把"改进"和"推广"拆成独立文件？

**问题**："改进"是修复当前模型的缺点，"推广"是把模型用到新场景。两者逻辑不同：
- 改进必须对应缺点（每条改进回应一条缺点）
- 推广不需要对应缺点（是模型能力的延伸）

拆分后可以检查"改进→缺点"的对应关系是否完整。

---

## 八、稳定运行讨论：具体怎么做的

### 8.1 题目要求回顾

题目原文提到三个优化目标：**低排放、低电耗、稳定运行**。其中"稳定运行"没有给出明确定义和量化指标。

### 8.2 为什么不显式建模"稳定运行"？

**问题分析**：
1. "稳定运行"没有统一的数学定义——是指 $C_{out}$ 方差小？振打不频繁？不出现反电晕？参数不剧烈波动？
2. 如果强行定义一个指标（如 $\min \text{Var}(C_{out})$），需要时序模拟，而我们的模型是静态优化（分工况独立求解），没有时序动态
3. 加入第三个目标会使优化问题从单目标变多目标，求解复杂度大增，但收益不确定

**决策**：不显式建模，而是论证"当前单目标解已隐式保障稳定运行"。论证需要三条证据：

### 8.3 三条保障机制的详细论证

**机制1：双向偏离模型的结构性保障**

- **问题**：单向模型下优化器把 $T$ 压到下界→振打频率=1/T 极大→锤击机构疲劳寿命急剧缩短→不稳定
- **修复**：双向偏离使 $T_{ref}$ 成为效率最高点，优化器自然选 $T \approx T_{ref}$
- **证据**：6个工况最优解的 $T$ 全部等于历史中位数（$T_1=T_2=233$ s, $T_3=T_4=472$ s），没有压到下界
- **结论**：双向偏离模型从结构上消除了"过频振打"的不稳定因素

**机制2：鲁棒优化的裕度验证**

- **问题**：即使当前解满足 $C_{out} \leq 10$，如果入口条件波动，$C_{out}$ 可能越界→不稳定
- **做法**：用机会约束 $\Pr(C_{out} \leq C_{limit}) \geq 95\%$ 重新优化，参数不确定性 $\sigma \approx 0.25$ mg/Nm³（来自贝叶斯后验标准差）
- **结果**：鲁棒最优解的电耗仅比确定性最优解高 0.30%
- **含义**：0.30% 的代价说明当前解离约束边界有足够裕度，入口条件波动 0.25 mg/Nm³ 不会导致超标
- **结论**：当前解对扰动不敏感，稳定裕度充足

**机制3：Pareto 前沿的权衡空间分析**

- **问题**：如果低排放和低峰值（稳定运行的另一个侧面）严重冲突，单目标优化可能给出"电耗低但峰值高"的不稳定解
- **做法**：以 $(P_{total}, C_{peak})$ 为双目标求 Pareto 前沿
- **结果**：$C_{peak}$ 降 80% 仅需 +0.2% 电耗，Pareto 前沿几乎水平
- **含义**：低电耗和低峰值几乎不冲突——单目标 $\min P$ 的解自动接近多目标的最优
- **结论**：三目标权衡空间宽裕，无需显式多目标

### 8.4 写作位置选择

**为什么放在问题4末尾而不是问题2？**

| 放在问题2 | 放在问题4 |
|-----------|-----------|
| 问题2的任务是"求解"，讨论稳定运行会打断求解叙事 | 问题4已经做了收紧分析，顺势讨论"更严约束下是否还稳定" |
| 鲁棒优化和 Pareto 前沿的结果还没计算 | 可以直接引用已计算的结果 |
| 逻辑上"稳定运行"是对所有问题的要求，不独属问题2 | 问题4是"最严苛情景"，在此讨论稳定性最有说服力 |

---

## 九、公式编号与交叉引用的管理

### 9.1 为什么不在 MD 文件中硬编码公式编号？

**问题**：LaTeX 自动编号，但写作时经常引用"式(3)"、"式(7)"等。如果中间插入新公式，所有后续编号都会变。

**决策**：
- 论文 .tex 中用 `\label` 和 `\ref`，让 LaTeX 自动管理
- MD 文档中用描述性引用（如"电耗模型式"而非"式(3)"），避免硬编码

### 9.2 当前公式编号偏移说明

问题1的 5.1.2 节新增了振打功耗公式（式4）和双向偏离公式（式5-7），导致后续公式编号整体后移约3个。正文中所有 `\ref` 引用会自动更新，无需手动修改。

---

## 十、LaTeX 编译方法

详见本文档末尾附录。

---

## 附录：LaTeX 编译完整教程

### A.1 环境准备

本项目使用 CUMCM（全国大学生数学建模竞赛）LaTeX 模板，需要以下环境：

| 组件 | 推荐方案 | 备注 |
|------|----------|------|
| TeX 发行版 | **TeX Live 2024+**（Windows/Mac/Linux）或 **MiKTeX** | 必须包含 XeLaTeX |
| 编辑器 | VS Code + LaTeX Workshop 插件 | 或 TeXstudio、Overleaf |
| 字体 | SimSun（宋体）、SimHei（黑体） | Windows 自带；Mac/Linux 需安装 |

### A.2 编译命令

打开终端，进入论文目录：

```bash
cd D:\WorkSpace\Problem\Paper
```

**单次编译**（快速预览，交叉引用可能不正确）：

```bash
xelatex main.tex
```

**完整编译**（推荐，确保引用、目录、编号全部正确）：

```bash
xelatex main.tex
xelatex main.tex
bibtex main
xelatex main.tex
xelatex main.tex
```

> 为什么需要编译4次？
> - 第1次：生成 .aux 文件（记录交叉引用信息）
> - 第2次：根据 .aux 更新引用编号
> - bibtex：处理参考文献
> - 第3-4次：确保所有引用和编号最终一致

### A.3 VS Code 配置（推荐）

在 VS Code 中安装 **LaTeX Workshop** 插件后，在 `.vscode/settings.json` 中添加：

```json
{
  "latex-workshop.latex.tools": [
    {
      "name": "xelatex",
      "command": "xelatex",
      "args": ["-synctex=1", "-interaction=nonstopmode", "-file-line-error", "%DOC%"]
    }
  ],
  "latex-workshop.latex.recipes": [
    {
      "name": "xelatex × 2",
      "tools": ["xelatex", "xelatex"]
    },
    {
      "name": "xelatex -> bibtex -> xelatex × 2",
      "tools": ["xelatex", "bibtex", "xelatex", "xelatex"]
    }
  ]
}
```

保存 .tex 文件后 LaTeX Workshop 会自动编译，点击右上角绿色播放按钮可查看 PDF。

### A.4 常见编译错误及解决

| 错误信息 | 原因 | 解决 |
|----------|------|------|
| `Font 'SimSun' not found` | 缺少中文字体 | Windows: 确认 C:\Windows\Fonts 有 simsun.ttc；Mac: 安装宋体 |
| `Undefined control sequence \zihao` | 缺少 ctex 宏包 | 确认 TeX Live 安装了 ctex：`tlmgr install ctex` |
| `File 'format.cls' not found` | 缺少 CUMCM 模板类文件 | 确认 `format.cls` 在 Paper 目录下 |
| `Missing $ inserted` | 数学符号未在数学模式中 | 检查是否在 $...$ 或 \[...\] 中使用下标、希腊字母等 |
| `Too many }'s` | 大括号不匹配 | 检查 \textbf{}、\frac{}{} 等嵌套 |

### A.5 图片路径

论文中图片引用路径为 `figures/xxx.png`，实际图片在 `D:\WorkSpace\Problem\outputs\` 下。编译前需要：

```bash
# 在 Paper 目录下创建 figures 链接指向 outputs
mklink /D figures ..\outputs
```

或者直接把 outputs 下的 png 文件复制到 `Paper\figures\` 目录。

### A.6 快速验证编译

```bash
cd D:\WorkSpace\Problem\Paper
xelatex main.tex
```

如果无报错且生成 `main.pdf`，说明编译成功。打开 PDF 检查：
1. 中文是否正常显示
2. 公式编号是否连续
3. 图片是否正确插入
4. 参考文献是否显示